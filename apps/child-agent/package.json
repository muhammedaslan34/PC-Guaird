{
  "name": "child-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cargo run --manifest-path src-tauri/Cargo.toml",
    "build": "cargo build --manifest-path src-tauri/Cargo.toml",
    "build:installer": "cargo build --manifest-path src-tauri/Cargo.toml --release --bin child-agent-service --bin child-agent-tray && powershell -ExecutionPolicy Bypass -File windows/build-installer.ps1",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "lint": "cargo fmt --manifest-path src-tauri/Cargo.toml --check"
  }
}
